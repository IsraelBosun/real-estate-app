import React from 'react'

import { BiBed, BiBath, BiArea } from "react-icons/bi";

const House = ({ house }) => {
  const { image, type, country, } = house;
  return (
    <div>
      <img src={""} alt="" />
    </div>
  )
}

export default House
